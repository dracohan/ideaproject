需求：有太多的类需要实现，每一个类都有差不多的接口，但又不一样，只是简单的调整一下其中的牛奶价格，就要每一个都查一遍
填坑：把其中的一些公用的东西抽象出基类来，其他的类继承。但是添加新的功能需要改动基类。
    并不是所有的东西都是统一的可以作为基类的，有些这个有，有些那个有
原则：做到对扩展开放，对修改关闭,扩展类型必须按照固定的格式

实现：
模式：给予对象动态增加特性的能力，可以用来灵活的扩展功能