需求：创建新的对象
填坑：直接用new等于自找烦恼，因为每次有新的对象创建都需要改变代码，代码并非对修改关闭
原则：把创建从创建者中抽象出来，形成一个接口，这个接口可以在接口的继承类中有不同的实现，这样当有新的产品的时候，创建者的代码是不变的

实现：方法一，定义简单的工厂类来实现创建部分，但是这样的坏处是不能通过继承修改创建行为（可能有不同的披萨店）
    方法二，在披萨店基类中定义一个抽象创建函数，在每一个子类中去实现它，这样可以有不同的创建行为
模式：工厂方法模式定义一个创建对象的接口，让子类决定如何实例化