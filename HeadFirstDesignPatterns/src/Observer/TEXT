需求：有3个函数获得温度，湿度和压力，做到动态显示这三个值
填坑：不能直接针对实现编程，否则每当有新的显示方式加入时都要修改实现代码。
原则：做到订阅者和订阅对象的低耦合，订阅对象只知道订阅者有某一些接口
    可以很方便的加入新的订阅者，当有新的订阅者时，被订阅对象不需要改动什么，只需要订阅者实现某一些（所有订阅者都有的）接口就行
    改动订阅者和订阅对象实现不会影响彼此
实现：将订阅主题放在一个类中，并用组合实现订阅对象，当有新的更新时，通知每一个订阅者。订阅者可以动态的添加和删除
模式：定义一对多的关系，当变化时通知这些客户。